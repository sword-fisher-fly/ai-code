# 2048 游戏 - 10x10 网格版

基于 Go 语言和 Ebiten 游戏引擎实现的 2048 游戏，采用更大的 10x10 网格，初始时有 8 个随机数字格子。

![Go](https://img.shields.io/badge/Go-1.21-blue)
![Ebiten](https://img.shields.io/badge/Ebiten-v2.7.5-lightgrey)

## 游戏特性

- 🎮 经典的 2048 游戏玩法
- 📐 10x10 大网格（标准版本为 4x4）
- 🎯 初始 8 个带数字的格子
- 🎨 美观的界面设计
- ⌨️ 支持键盘控制（WASD 或方向键）
- 🔄 支持游戏重启
- 📊 实时分数显示

## 游戏规则

1. 使用 **方向键** 或 **WASD** 移动数字方块
2. 相同数字的方块碰撞时会合并成更大的数字
3. 每次移动后，会在空位置随机生成一个新方块（2 或 4）
4. 目标是合成 **2048** 方块！
5. 当网格填满且无法移动时，游戏结束
6. 按 **R** 键重新开始游戏

## 系统要求

### 开发环境
- Go 1.21 或更高版本
- Git

### 系统依赖

#### Linux
需要安装以下 X11 开发库：
```bash
# Ubuntu/Debian
sudo apt-get install libx11-dev libxrandr-dev libxcursor-dev libxi-dev libxinerama-dev libgl1-mesa-dev

# CentOS/RHEL/Fedora
sudo yum install libX11-devel libXrandr-devel libXcursor-devel libXi-devel libXinerama-devel mesa-libGL-devel

# Arch Linux
sudo pacman -S libx11 libxrandr libxcursor libxi libxinerama mesa
```

#### macOS
需要安装 Homebrew 和 pkg-config：
```bash
brew install pkg-config
```

#### Windows
推荐使用 TDM-GCC 或 MinGW-w64。

## 快速开始

### 1. 克隆项目
```bash
git clone <your-repo-url>
cd ai-golang
```

### 2. 安装依赖
```bash
# 安装Go依赖
make deps

# 安装系统依赖（自动检测OS）
make install-deps
```

### 3. 运行游戏
```bash
# 方式1：使用Makefile
make run

# 方式2：直接运行
go run .
```

### 4. 构建可执行文件
```bash
# 构建当前平台版本
make build

# 构建所有平台版本
make build-all

# 构建特定平台
make build-linux
make build-windows
make build-macos
```

## 构建说明

### 使用 Makefile（推荐）

```bash
# 查看所有可用命令
make help

# 完整构建流程
make deps && make install-deps && make build && make run
```

### 手动构建

```bash
# 下载依赖
go mod tidy

# 构建
go build -o game2048

# 运行
./game2048
```

## 项目结构

```
ai-golang/
├── main.go         # 主游戏文件
├── go.mod          # Go模块文件
├── Makefile        # 跨平台构建脚本
└── README.md       # 项目说明文档
```

## 游戏控制

| 按键 | 功能 |
|------|------|
| ← / A | 向左移动 |
| → / D | 向右移动 |
| ↑ / W | 向上移动 |
| ↓ / S | 向下移动 |
| R | 重新开始游戏 |

## 技术实现

### 核心技术栈
- **Go 1.21** - 编程语言
- **Ebiten v2.7.5** - 2D游戏引擎
- **Go Modules** - 依赖管理

### 游戏架构
- **Game** - 主游戏状态管理
- **Grid** - 10x10 网格系统
- **Tile** - 数字方块系统
- **Input Handler** - 输入处理系统
- **Renderer** - 渲染系统

### 核心功能
- [x] 网格初始化（10x10）
- [x] 随机方块生成
- [x] 四方向移动逻辑
- [x] 方块合并算法
- [x] 碰撞检测
- [x] 游戏结束检测
- [x] 分数系统
- [x] 键盘输入处理
- [x] 图形渲染

## 常见问题

### Q: Linux 编译时提示缺少 X11 库？
A: 运行 `make install-deps` 安装系统依赖，或手动安装 X11 开发库。

### Q: macOS 上编译失败？
A: 确保已安装 Xcode Command Line Tools 和 Homebrew。

### Q: Windows 上无法运行？
A: Windows 需要 CGO 支持，确保使用 MinGW 或 TDM-GCC 编译。

### Q: 游戏运行缓慢？
A: 确保系统安装了图形驱动和 OpenGL 支持。

## 开发计划

- [ ] 添加动画效果
- [ ] 添加声音效果
- [ ] 添加高分记录
- [ ] 添加触屏支持
- [ ] 添加难度设置
- [ ] 添加主题切换

## 许可证

MIT License

## 贡献

欢迎提交 Issue 和 Pull Request！

## 致谢

- [Ebiten](https://ebiten.org/) - 优秀的 Go 2D 游戏引擎
- [2048](https://github.com/gabrielecirulli/2048) - 原版游戏创意